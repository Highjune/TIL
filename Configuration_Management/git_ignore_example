# https://github.com/github/gitignore 사이트에 찾을 수 있으
## 아래 예시들과 설명들은 책 `프로 Git 2/E` 에서 공부하며 적은 것들

# 기본적인 패턴
- 아무것도 없는 라인이나, `샾` 으로 시작되는 라인은 무시한다.
- 표준 Glob 패턴을 사용한다.
- 슬래시(/) 로 시작하면 하위 디렉터리에 적용되지(recursivity) 않는다.
- 디렉터리는 실래시(/)를 끝에 사용하는 것으로 표현한다.
